# Grafana 대시보드 자동 프로비저닝 설정

apiVersion: 1

providers:
  - name: 'FairBid Dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
