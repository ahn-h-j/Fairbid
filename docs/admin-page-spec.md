# 관리자 페이지 설계 명세서

> 작성일: 2025-01-28
> 상태: 설계 완료, 구현 대기

---

## 1. 개요

### 1.1 목적
FairBid 플랫폼의 운영 및 데이터 분석을 위한 관리자 전용 페이지

### 1.2 운영 시나리오
- **사용자**: 1인 운영 (개발자 본인)
- **보안 수준**: MVP 단계 (낮음)
- **주요 업무**: 데이터 분석, 경매 운영 조작

### 1.3 핵심 원칙
- 기존 FairBid UI 재사용 (일관된 디자인 시스템)
- 빠른 구현 우선 (MVP)
- 확장 가능한 구조

---

## 2. 기능 요구사항

### 2.1 대시보드 (통계)

#### 핵심 지표 (MVP)

| 카테고리 | 지표 | 설명 | 계산 방식 |
|---------|------|------|----------|
| **경매 효율** | 낙찰률 | 등록된 경매 중 낙찰 완료 비율 | `낙찰완료 / 전체등록 * 100` |
| | 평균 경쟁률 | 경매당 평균 입찰자 수 | `총입찰자수 / 경매수` |
| | 평균 상승률 | 시작가 대비 낙찰가 상승률 | `(낙찰가 - 시작가) / 시작가 * 100` |
| **시간 패턴** | 피크 시간대 | 입찰이 가장 활발한 시간대 | 시간대별 입찰 수 집계 |
| | 연장 발생률 | 경매 연장 발생 비율 | `연장발생경매 / 전체경매 * 100` |

#### 데이터 조회
- **기간 선택**: 7일 / 30일 / 전체 (프리셋 버튼)
- **시각화**: Recharts 라이브러리
  - 숫자 카드 (KPI)
  - 시계열 차트 (일별 추이)
  - 바 차트 (시간대별 분포)

### 2.2 경매 관리

#### 경매 목록
- **조회**: 검색 (제목, 판매자) + 상태 필터 조합
- **상태 필터**: 전체 / 진행중 / 종료 / 낙찰완료 / 유찰
- **정렬**: 최신순 / 마감임박순 / 입찰수순

#### 운영 도구 (기존 테스트 도구 이전)
| 기능 | 설명 | 확인 방식 |
|------|------|----------|
| 5분 후 마감 설정 | 경매 종료 시간을 5분 후로 설정 | confirm |
| N초 후 마감 설정 | 종료 시간을 N초 후로 설정 | confirm |
| 강제 종료 | 경매를 즉시 종료 | confirm |
| 캐시 새로고침 | Redis 캐시 갱신 | confirm |
| 노쇼 강제 처리 | 결제 기한 만료 + 노쇼 처리 | confirm |

#### 경매 상세 - 입찰 이력 (CS 대응용)
| 정보 | 설명 |
|------|------|
| 입찰자 | 닉네임 + 이메일 (클릭 시 유저 상세로 이동) |
| 입찰가 | 입찰 금액 |
| 입찰 시각 | YYYY-MM-DD HH:mm:ss |
| 순위 | 현재 순위 (1위, 2위, ...) |
| 상태 | 낙찰 / 패찰 / 노쇼 / - |

> **용도**: "왜 내가 낙찰 안 됐어요?" 같은 CS 문의 대응 시 입찰 이력 확인 필수

### 2.3 유저 관리

#### 유저 목록
- **조회**: 검색 (닉네임, 이메일) + 상태 필터
- **표시 정보**: 닉네임, 이메일, 가입일, 경고 횟수, 차단 여부
- **상태 필터**: 전체 / 정상 / 경고있음 / 차단됨

#### 기능 (MVP)
- 조회만 지원 (경고 초기화, 차단/해제는 추후)

#### 유저 상세 - 활동 이력 (CS 대응용)
| 섹션 | 내용 |
|------|------|
| **기본 정보** | 닉네임, 이메일, 전화번호, 가입일, OAuth Provider |
| **상태 정보** | 경고 횟수, 차단 여부, 온보딩 완료 여부 |
| **판매 활동** | 등록한 경매 목록 (제목, 상태, 낙찰가) |
| **구매 활동** | 입찰한 경매 목록 (제목, 입찰가, 결과) |
| **노쇼 이력** | 노쇼 발생 경매 목록 (경매 제목, 발생일, 경고 부여 여부) |

> **용도**: 특정 유저 관련 CS 문의 대응, 악성 유저 판별

---

## 3. 기술 설계

### 3.1 권한 시스템

#### User 도메인 변경
```java
// UserRole enum 추가
public enum UserRole {
    USER,   // 일반 사용자
    ADMIN   // 관리자
}

// User 도메인에 role 필드 추가
private UserRole role;  // 기본값: USER
```

#### ADMIN 부여 방식
```yaml
# application.yml
app:
  admin:
    emails: ${ADMIN_EMAILS:}  # 콤마 구분 이메일 목록
```

```java
// AuthService - OAuth 로그인 시 ADMIN 자동 부여
@Value("${app.admin.emails:}")
private String adminEmails;

private UserRole determineRole(String email) {
    List<String> admins = Arrays.asList(adminEmails.split(","));
    return admins.contains(email.trim()) ? UserRole.ADMIN : UserRole.USER;
}
```

#### JWT에 role 포함
```java
// JwtTokenProvider
claims.put("role", user.getRole().name());
```

#### 권한 체크 (2중 검증)
1. **JWT 필터**: role 클레임 검증
2. **@PreAuthorize**: 컨트롤러 메서드별 검증

```java
@RestController
@RequestMapping("/api/v1/admin")
@PreAuthorize("hasRole('ADMIN')")
public class AdminController {
    // ...
}
```

### 3.2 API 설계

#### 통계 API
```
GET /api/v1/admin/stats/overview?period={7d|30d|all}
GET /api/v1/admin/stats/auctions?period={7d|30d|all}
GET /api/v1/admin/stats/time-pattern?period={7d|30d|all}
```

#### 경매 관리 API
```
GET  /api/v1/admin/auctions?search={}&status={}&page={}&size={}
GET  /api/v1/admin/auctions/{id}                              # 경매 상세 (입찰 이력 포함)
GET  /api/v1/admin/auctions/{id}/bids                         # 입찰 이력 조회
POST /api/v1/admin/auctions/{id}/set-end-time?seconds={}
POST /api/v1/admin/auctions/{id}/force-close
POST /api/v1/admin/auctions/{id}/refresh-cache
POST /api/v1/admin/auctions/{id}/force-noshow
```

#### 유저 관리 API
```
GET /api/v1/admin/users?search={}&status={}&page={}&size={}
GET /api/v1/admin/users/{id}                                  # 유저 상세 (활동 이력 포함)
GET /api/v1/admin/users/{id}/auctions                         # 유저가 등록한 경매
GET /api/v1/admin/users/{id}/bids                             # 유저의 입찰 이력
GET /api/v1/admin/users/{id}/noshows                          # 유저의 노쇼 이력
```

### 3.3 프론트엔드 구조

#### 라우팅
```
/admin                    → AdminLayout (사이드바 포함)
  /admin/dashboard        → DashboardPage (대시보드)
  /admin/auctions         → AuctionManagePage (경매 관리)
  /admin/auctions/:id     → AuctionDetailAdminPage (경매 상세 + 입찰 이력)
  /admin/users            → UserManagePage (유저 관리)
  /admin/users/:id        → UserDetailAdminPage (유저 상세 + 활동 이력)
```

#### 컴포넌트 구조
```
frontend/src/
├── pages/
│   └── admin/
│       ├── AdminLayout.jsx             # 사이드바 + Outlet
│       ├── DashboardPage.jsx           # 대시보드
│       ├── AuctionManagePage.jsx       # 경매 목록
│       ├── AuctionDetailAdminPage.jsx  # 경매 상세 (입찰 이력 + 운영도구)
│       ├── UserManagePage.jsx          # 유저 목록
│       └── UserDetailAdminPage.jsx     # 유저 상세 (활동 이력)
├── components/
│   └── admin/
│       ├── Sidebar.jsx             # 사이드바 네비게이션
│       ├── StatCard.jsx            # 통계 카드
│       ├── PeriodSelector.jsx      # 기간 선택 버튼
│       ├── AuctionTable.jsx        # 경매 테이블
│       ├── UserTable.jsx           # 유저 테이블
│       ├── BidHistoryTable.jsx     # 입찰 이력 테이블
│       ├── UserActivityTabs.jsx    # 유저 활동 탭 (판매/구매/노쇼)
│       └── charts/
│           ├── LineChart.jsx       # 시계열 차트
│           └── BarChart.jsx        # 바 차트
└── api/
    └── admin.js                    # Admin API 클라이언트
```

#### 접근 제어
```jsx
// Header.jsx - ADMIN이면 관리자 링크 노출
{user?.role === 'ADMIN' && (
  <Link to="/admin">관리자</Link>
)}

// AdminLayout.jsx - ADMIN 아니면 리다이렉트
const { user } = useAuth();
if (user?.role !== 'ADMIN') {
  return <Navigate to="/" replace />;
}
```

---

## 4. UI/UX 설계

### 4.1 레이아웃

```
┌─────────────────────────────────────────────────────┐
│  FairBid Admin                              [로그아웃] │
├──────────┬──────────────────────────────────────────┤
│          │                                          │
│  대시보드  │   [콘텐츠 영역]                           │
│          │                                          │
│  경매 관리 │                                          │
│          │                                          │
│  유저 관리 │                                          │
│          │                                          │
└──────────┴──────────────────────────────────────────┘
```

### 4.2 대시보드 화면

```
┌─────────────────────────────────────────────────────┐
│  기간: [7일] [30일] [전체]                            │
├─────────────────────────────────────────────────────┤
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐   │
│  │ 낙찰률   │ │ 평균경쟁률│ │ 평균상승률│ │ 연장발생률│   │
│  │  78.5%  │ │  4.2명  │ │  32.1%  │ │  45.3%  │   │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘   │
├─────────────────────────────────────────────────────┤
│  [일별 경매 현황 차트]                                │
│  ▁▂▃▅▆▇█▇▆▅▃▂▁                                     │
├─────────────────────────────────────────────────────┤
│  [시간대별 입찰 분포]                                 │
│  00 ▁ 06 ▂ 12 ▅ 18 █ 24                            │
└─────────────────────────────────────────────────────┘
```

### 4.3 경매 관리 화면

```
┌─────────────────────────────────────────────────────┐
│  [🔍 검색...]          상태: [전체 ▼]  [검색]         │
├─────────────────────────────────────────────────────┤
│  ID   │ 제목      │ 판매자  │ 현재가  │ 상태   │ 액션  │
│  123  │ 아이폰15  │ user1  │ 50,000 │ 진행중 │ [상세]│
│  122  │ 맥북프로  │ user2  │ 80,000 │ 종료   │ [상세]│
│  ...  │ ...      │ ...    │ ...    │ ...   │ ...  │
├─────────────────────────────────────────────────────┤
│  [< 이전]  1 2 3 4 5  [다음 >]                       │
└─────────────────────────────────────────────────────┘
```

### 4.4 경매 상세 (입찰 이력 + 운영 도구)

```
┌─────────────────────────────────────────────────────┐
│  ← 목록으로                                          │
├─────────────────────────────────────────────────────┤
│  경매 정보                                           │
│  ────────────────────────────────────────────────   │
│  제목: 아이폰15 프로                                  │
│  판매자: user1 (user1@email.com)        [유저 상세 →]│
│  시작가: 10,000원  │  현재가: 50,000원               │
│  상태: 진행중  │  마감: 2025-01-29 15:00            │
│  입찰 수: 12  │  연장 횟수: 2                        │
├─────────────────────────────────────────────────────┤
│  입찰 이력 (12건)                                    │
│  ────────────────────────────────────────────────   │
│  순위 │ 입찰자      │ 입찰가    │ 시각        │ 상태  │
│  1위  │ buyer3 →   │ 50,000원 │ 01-28 14:32 │ 낙찰  │
│  2위  │ buyer1 →   │ 48,000원 │ 01-28 14:30 │ 패찰  │
│  3위  │ buyer2 →   │ 45,000원 │ 01-28 13:15 │ 패찰  │
│  ...  │ ...        │ ...      │ ...         │ ...   │
├─────────────────────────────────────────────────────┤
│  운영 도구                                           │
│  ────────────────────────────────────────────────   │
│  [5분 후 마감] [강제 종료] [캐시 새로고침]              │
│                                                     │
│  [____] 초 후 마감 [적용]                            │
│                                                     │
│  [노쇼 강제 처리] (낙찰 완료 상태에서만 활성화)          │
└─────────────────────────────────────────────────────┘
```

### 4.5 유저 상세 (활동 이력)

```
┌─────────────────────────────────────────────────────┐
│  ← 목록으로                                          │
├─────────────────────────────────────────────────────┤
│  유저 정보                                           │
│  ────────────────────────────────────────────────   │
│  닉네임: buyer3              이메일: buyer3@test.com │
│  전화번호: 010-1234-5678     가입일: 2025-01-15      │
│  Provider: KAKAO             상태: 정상              │
│  경고: 1회 / 3회             차단: X                 │
├─────────────────────────────────────────────────────┤
│  [판매 활동]  [구매 활동]  [노쇼 이력]                 │
│  ─────────────────────────────────────────────────  │
│  (구매 활동 탭 선택됨)                                │
│                                                     │
│  경매 제목     │ 입찰가   │ 결과   │ 일시            │
│  아이폰15 →   │ 50,000원 │ 낙찰   │ 2025-01-28     │
│  맥북프로 →   │ 80,000원 │ 패찰   │ 2025-01-25     │
│  에어팟 →     │ 15,000원 │ 노쇼   │ 2025-01-20     │
│  ...          │ ...      │ ...    │ ...            │
└─────────────────────────────────────────────────────┘
```

---

## 5. 데이터베이스 변경

### 5.1 users 테이블
```sql
-- role 컬럼 추가
ALTER TABLE users ADD COLUMN role VARCHAR(20) NOT NULL DEFAULT 'USER';

-- 인덱스 (선택)
CREATE INDEX idx_users_role ON users(role);
```

### 5.2 UserEntity 변경
```java
@Enumerated(EnumType.STRING)
@Column(nullable = false, length = 20)
private UserRole role = UserRole.USER;
```

---

## 6. 구현 계획

### 6.1 Phase 1: 기반 작업
- [ ] UserRole enum 추가
- [ ] User 도메인/엔티티에 role 필드 추가
- [ ] JWT에 role 클레임 추가
- [ ] ADMIN_EMAILS 환경변수 처리
- [ ] AdminController 기본 구조 + 권한 체크

### 6.2 Phase 2: 프론트엔드 기반
- [ ] AdminLayout + 사이드바 구현
- [ ] 라우팅 설정
- [ ] Header에 관리자 링크 추가
- [ ] 권한 체크 (ADMIN 아니면 리다이렉트)

### 6.3 Phase 3: 대시보드
- [ ] 통계 API 구현 (overview, auctions, time-pattern)
- [ ] DashboardPage 구현
- [ ] StatCard, 차트 컴포넌트 구현

### 6.4 Phase 4: 경매 관리
- [ ] 경매 목록 API 구현
- [ ] AuctionManagePage 구현
- [ ] 경매 상세 API 구현 (입찰 이력 포함)
- [ ] AuctionDetailAdminPage 구현 (입찰 이력 테이블)
- [ ] 기존 테스트 도구 이전 (운영 도구로)
- [ ] 기존 AuctionDetailPage에서 테스트 도구 제거

### 6.5 Phase 5: 유저 관리
- [ ] 유저 목록 API 구현
- [ ] UserManagePage 구현
- [ ] 유저 상세 API 구현 (활동 이력 포함)
- [ ] UserDetailAdminPage 구현 (활동 탭: 판매/구매/노쇼)

---

## 7. 트레이드오프 및 결정 사항

| 항목 | 결정 | 이유 |
|------|------|------|
| role 저장 방식 | DB (User 테이블) | 확장성. 나중에 MODERATOR 등 추가 가능 |
| ADMIN 부여 | 환경변수 기반 자동 부여 | 1인 운영에 적합. DB 직접 수정 불필요 |
| 통계 캐싱 | 없음 (MVP) | 데이터 적음. 필요시 Redis 캐싱 추가 |
| 차트 라이브러리 | Recharts | React 친화적, 커스터마이징 용이 |
| 유저 관리 기능 | 조회만 (MVP) | 경고 초기화/차단은 추후 필요시 추가 |

---

## 8. 보안 고려사항

### MVP 단계 (현재)
- JWT + @PreAuthorize 2중 검증
- confirm 대화상자로 실수 방지
- HTTPS 필수 (프로덕션)

### 추후 고려 (프로덕션)
- 관리자 작업 감사 로그 (audit log)
- 위험 작업 2차 인증
- IP 화이트리스트

---

## 9. 참고사항

### 기존 테스트 도구 위치
- `frontend/src/pages/AuctionDetailPage.jsx`
  - `TestTools` 컴포넌트 (L320-404)
  - `TestNoShowButton` 컴포넌트 (L417-483)

### 관련 API (기존)
- `POST /api/v1/test/auctions/{id}/set-ending-soon`
- `POST /api/v1/test/auctions/{id}/force-close`
- `POST /api/v1/test/auctions/{id}/refresh-cache`
- `POST /api/v1/test/auctions/{id}/set-end-time?seconds={}`
- `POST /api/v1/test/auctions/{id}/force-noshow`

→ `/api/v1/admin/auctions/{id}/*` 로 이전 예정
